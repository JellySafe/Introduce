# Design — JellySafe 소개 사이트

<!-- 스택: Next.js 16 App Router · React 19 · TypeScript · output: "export".
     아래 파일 경로는 전부 Next 구조 기준이다. -->

<!-- 이 문서의 권한 범위는 `Landing/` 하나다. FrontEnd 모노레포의 제품 UI는
     `packages/design-system` 이 따로 소유한다. 여기 적힌 값은 그 디자인 시스템의
     primitive 를 그대로 옮겨 쓰되, 마케팅 표면에만 필요한 것(디스플레이 스케일,
     시각 밴드, 디오라마)을 덧붙인 것이다. 두 문서가 충돌하면 제품 쪽이 우선한다. -->

빌드된 결과에서 역으로 기록했다. 의도가 아니라 화면에 실제로 있는 값이다.

## 1. 지배 개념 — 시각이 지면을 정한다

이 페이지는 성수기 하루 한 날(06:20 → 19:40)이다. 스크롤 진행도가 곧 시각이고,
그 시각이 **지면 색과 3D 디오라마의 태양·그림자·조명을 동시에** 정한다.
다크/라이트는 취향으로 고른 것이 아니라 장면이 강제한 값이다.

    스크롤 0 ────────────────────────────────────────────── 1
    06:20   07:30   09:00  11:30  13:00  14:10  16:00  17:00  19:40
    ■어두움 ■어두움 ■어두움 □밝음  □밝음  □밝음  ■어두움 ■어두움 ■어두움
    └─ 디오라마 보임 ─┘        └─ 불투명(캔버스 정지) ─┘   └ 디오라마 보임 ┘

구간 경계는 절대 칼자국으로 남기지 않는다. `[data-from]` 이 앞 구간의 색에서
220px 에 걸쳐 시작하고, `.station--rise` / `.station--fade` 가 디오라마와
불투명 지면 사이를 잇는다.

## 2. 색

### 원시 토큰
`../FrontEnd/packages/design-system/src/styles/primitives.css` 를 그대로 옮겼다.
값은 `src/app/globals.css` 에 산다.
브랜드 `--primary-50 #256af4`. 위험 4색은 **의미 토큰이라 임의로 바꾸지 않는다**.

### 시각별 지면
| 토큰 | 값 | 시각 |
|---|---|---|
| `--hr-0620` | `#070d1a` | 06:20 |
| `--hr-0900` | `#0e1e33` | 09:00 |
| `--hr-1130` | `#f6f9fd` | 11:30 |
| `--hr-1300` | `#ffffff` | 13:00 |
| `--hr-1410` | `#f5f8fc` | 14:10 |
| `--hr-1600` | `#102338` | 16:00 |
| `--hr-1700` | `#0a1729` | 17:00 |
| `--hr-1940` | `#060e1c` | 19:40 |

### 밴드 의미 토큰
`[data-band="dark"]` 는 `:root` 에도 걸려 있다. **이게 없으면 `body { color: var(--ink) }`
가 무효값이 되어 색을 명시하지 않은 모든 글자가 검정으로 계산된다** — 실제로 한 번
그렇게 났고, 그 재발을 막으려고 두 곳에 둔다.

| | dark | light |
|---|---|---|
| `--ink` | `#e9eff8` | `#0b0d0e` |
| `--ink-2` | `#9db0ca` | `#444c55` |
| `--ink-3` | `#7d92b5` | `#67717e` |
| `--brand` | `--primary-40` | `--primary-60` |
| `--rule` | `rgba(160,195,255,.14)` | `#e2e8f0` |

### 위험 4색 — 글자와 면을 분리한다
대비 기준이 다르므로 토큰도 둘이다. 밝은 지면에서 `#ffd014`(주의)를 글자에 쓰면
2.8:1 이라 못 쓴다. 반대로 막대를 올리브(`#8a6e00`)로 칠하면 등급이 색으로 안 읽힌다.

| 등급 | 글자 `--r-*` (dark / light) | 면 `--f-*` (dark / light) |
|---|---|---|
| 안전 | `#3fa674` / `#26734e` | `#3fa674` / `#228756` |
| 주의 | `#ffd014` / `#8a6e00` | `#ffd014` / `#ffd014` |
| 위험 | `#f5683d` / `#c2350a` | `#f5683d` / `#e43e0c` |
| 심각 | `#f04256` / `#bd0f23` | `#f04256` / `#de122a` |

모든 본문 텍스트의 대비는 렌더에서 실측해 4.5:1(큰 글자 3:1) 이상을 확인했다
(렌더 실측, 32곳). 눈으로 판정하지 않는다.

## 3. 서체 — Pretendard 한 벌

브랜드 약속이라 두 번째 서체를 들이지 않는다. **계기 조판은 서체가 아니라
`font-variant-numeric: tabular-nums slashed-zero` 와 `letter-spacing: 0.17em`
대문자 라벨로 만든다.** 모노스페이스를 "기술적으로 보이려고" 쓰지 않는다.

| 역할 | 크기 | 행간 | 자간 | 굵기 |
|---|---|---|---|---|
| `.display` (히어로) | `clamp(2.3rem, 4.5vw, 3.9rem)` | 1.08 | −0.037em | 700 |
| `.close__title` | `clamp(2.05rem, 4.3vw, 3.4rem)` | 1.20 | −0.031em | 700 |
| `.h2` | `clamp(1.85rem, 3.7vw, 2.95rem)` | 1.14 | −0.032em | 660 |
| `.lede` | `clamp(1.06rem, 1.45vw, 1.28rem)` | 1.66 | −0.016em | 400, max 42ch |
| 본문 | 17px | 1.75 | — | 400, max 68ch |
| `.inst-label` | 0.685rem | 1.3 | +0.17em, 대문자 | 620 |

`word-break: keep-all` — 한국어를 음절 단위로 끊지 않는다.

**행간은 라틴 기준으로 잡으면 안 된다.** 한글은 글자가 상자를 꽉 채워 디센더
여유가 없다. 디스플레이를 1.02 로 두었더니 57.6px 글자에 실질 여백이 1.15px 뿐이라
줄이 맞붙었다. 그리고 **행이 늘수록 필요한 행간도 는다** — 그래서 3~4행이 나오는
닫는 절 제목(`.close__title`)은 히어로보다 풀어 둔다.

## 4. 간격

`--s1 6 · --s2 10 · --s3 16 · --s4 24 · --s5 36 · --s6 56 · --s7 88 · --s8 132`.
하나의 스케일만 쓴다. 구간 상하 여백은 `--s8`(모바일 `--s7`), 제목과 본문 사이는
`--s7`. 제목 위가 아래보다 넓다.

## 5. 구조 문법

**카드가 아니라 규칙선과 행이 구조를 만든다.** 아이콘+제목+본문 카드 3단 그리드는
쓰지 않는다. 눈썹(kicker)도 쓰지 않는다 — 제목이 스스로 선다.
시각 스탬프(`.stamp`)는 예외다. 시퀀스(하루)가 정보 자체라서 시각 표기가 장식이 아니다.

| 컴포넌트 | 쓰는 자리 |
|---|---|
| `.rail` | 좌측 고정 시각 레일(모바일은 하단 바). 페이지의 척추 |
| `.gap` | 마주 놓는 두 값 (07:30 어긋남) |
| `.feeds` / `.feed` | 자료원 목록. `.beat` 눈금 간격 = 갱신 주기 |
| `.levels` / `.level` | 위험 4단계. 왼쪽 색 띠가 순서 있는 척도임을 말한다 |
| `.play` | 룰 플레이그라운드. `.beach__meter` 는 45·76 컷오프에 실선 |
| `.pipe` / `.step` | 분기 없는 단선 파이프라인 |
| `.split` | 두 서비스 대비 (16:00) |
| `.ledger` | 개정 전후 측정 원장. **안 바뀐 행도 싣는다** |
| `.revs` / `.rev` | 버전 결정 기록 |
| `.cannots` | 못 하는 것. 각주가 아니라 정식 섹션 |
| `.mock` | 앱 화면 목업. 제품의 라이트 토큰을 그대로 쓴다 |

클래스는 전부 `src/app/globals.css` 에 있고, 마크업은 `src/features/stations/*.tsx`
가 소유한다. 구간 하나 = 파일 하나다.

## 6. 모션 — authored moment 하나

**하루가 지나가는 것**이 이 페이지의 유일한 연출이다. 그 외에는
`[data-in]` 의 한 가지 등장(14px 상승 + 페이드, 0.72s, `cubic-bezier(.16,1,.3,1)`)
뿐이고, 형제끼리 55ms 씩만 어긋난다. 섹션마다 다른 연출은 리듬이 아니라 산만함이다.

레이아웃 속성은 애니메이션하지 않는다 — `.beach__meter` 의 길이도 `width` 가 아니라
`transform: scaleX()` 다. `prefers-reduced-motion` 이면 등장이 사라지고 디오라마는
정적 폴백으로 대체된다.

**등장 연출은 `@media (scripting: enabled)` 안에만 산다.** 자바스크립트가 없으면
관찰자가 못 돌아 본문이 영영 숨겨지기 때문이다. 예전에는 인라인 스크립트가
`<html>` 에 `.js` 를 붙여 이 분기를 만들었는데, **`<html>` 의 className 은 React 가
소유해서 하이드레이션 불일치가 났다.** 미디어 질의는 DOM 을 건드리지 않고 같은
분기를 만들고, 이 특성을 모르는 브라우저에서는 질의가 안 맞아 본문이 그냥 보인다
— 실패해도 열리는 쪽이다.

## 7. 브라우저 표면

선택 영역, 스크롤바, 포커스 링, 밑줄 오프셋, 표 숫자를 전부 팔레트에서 칠했다.
브라우저 기본값은 어느 디자인 시스템에도 속하지 않는다.

## 8. 3D 디오라마 (`src/features/diorama/beach-scene.ts`)

- **스케일 규약 1 unit = 1 m.** 사람 1.72 · 파라솔 2.10H · 망대 3.20H · 부이 1.30∅.
  이 규약이 없으면 블록 크기가 감으로 정해져 "장난감"으로 읽힌다.
- **해안 단면은 함수 하나**(`sandY(z)`)다. z=0 이 물가이고 뭍은 올라가고 바다는
  내려간다. 지오메트리와 소품 배치가 같은 함수를 봐야 물건이 뜨거나 박히지 않고,
  "물가가 어디냐"에 답이 하나만 존재한다.
- **시야**: 망원 원근(fov 23, 50 m). 순수 직교는 도면처럼 납작해지고 사람이
  사람으로 안 읽힌다. 전경을 다 담으면 사람이 점이 되므로 해변 한 구획만 잡는다.
- **프레이밍은 재서 맞춘다.** 콘솔에서 `jellysafeDiorama.probe()` 가 망대·부이의
  화면 좌표(%)를 돌려준다. `panX` 는 이 값으로 정했다(망대 ≈ 65% x).
- **깃발이 곧 등급이다.** 06:20 초록(안전) → 14:10 제보 접수 후 노랑(주의).
  파라솔은 아침에 펴고 저녁에 접는다. 하루가 흐른다는 가장 싼 증거다.
- 후처리는 폭 900px 이상에서만(SMAA + Bloom + OutputPass). 캔버스가 가려지는
  구간에서는 렌더 루프를 세운다.
- **파도는 정점 셰이더가 만든다.** 예전에는 매 프레임 자바스크립트로 정점을 흔들고
  `computeVertexNormals()` 로 법선을 다시 구했는데, 삼각형 23,040 개를 CPU 로 도느라
  프레임 예산의 55%(5.3ms)를 썼다. 파형 식을 알고 있으니 법선은 해석적 미분으로
  바로 구한다 — 갱신 비용이 0 이 됐다.
- **그림자 맵은 자동 갱신을 끈다.** 태양은 스크롤할 때만 움직이고 사람의 출렁임은
  9cm 라, 시각이 바뀌었거나 6프레임이 지났을 때만 다시 굽는다.
- **안 쓸 것은 받지 않는다.** 동작 줄이기·WebGL2 없음은 `DayScene` 이 동적 import
  **전에** 판정한다. 예전에는 three.js 청크(599KB)를 받아 파싱한 뒤에야
  `beach-scene` 안에서 확인했다.
- **캔버스에 글자를 그리지 않는다.** 카피는 전부 DOM 이라 SEO·스크린리더·다국어에
  손실이 없다.

## 9. 실패 시 동작

WebGL2 미지원, `prefers-reduced-motion`, 모듈/three.js 사본 부재 — 어느 경우든
`.sky__fallback` 정적 배경이 뜨고 카피는 그대로 읽힌다. 외부 CDN 을 참조하지 않는다.

## 9b. 서버/클라이언트 경계

**클라이언트로 내려가는 것은 둘뿐이다.**

| | 왜 |
|---|---|
| `DayScene` | 스크롤·IntersectionObserver·WebGL 을 소유한다. 안의 값은 상태가 아니라 ref 로 직접 쓴다 |
| `RulePlayground` | 이 페이지에서 유일하게 진짜 상태(관측값 5개 → 해변 12곳)가 있는 자리 |

나머지 10개 구간은 서버 컴포넌트라 본문에 자바스크립트가 실리지 않는다.
새 구간을 만들 때 `"use client"` 를 붙이려거든, 먼저 그 자리에 정말 상태가
있는지 물어라 — 이 페이지에서는 대개 없다.

## 10. 이 세계가 거부하는 것

- 눈썹(kicker) — 제목이 스스로 선다
- 아이콘+제목+본문 카드 3단 그리드
- 히어로-지표 템플릿(큰 숫자 + 작은 라벨 타일)
- 그라디언트 텍스트
- "기술적으로 보이려고" 쓰는 모노스페이스
- 아이콘 자리를 대신하는 유니코드 글리프·이모지 (아이콘은 1.5 선 두께로 직접 그렸다)
- 저장소에 없는 수치. 시연용 예시는 시연용이라고 화면에 적는다
